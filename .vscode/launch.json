{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Jalankan HTML",
            "type": "chrome",
            "request": "launch",
            "file": "${file}"
        }
    ]
}